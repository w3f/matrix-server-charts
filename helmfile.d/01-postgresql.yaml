environments:
  production:
  ci:

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
- name: postgresql
  chart: bitnami/postgresql
  version: 10.12.3
  values:
  - ./config/postgresql-values.yaml.gotmpl
