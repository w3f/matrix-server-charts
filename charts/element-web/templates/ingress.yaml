apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: element-web
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/limit-rps: "10"
spec:
  tls:
  - hosts:
      - riot.{{ .Values.domain }}
    secretName: element-web-tls
  rules:
  - host: riot.{{ .Values.domain }}
    http:
      paths:
      - path: /
        backend:
          serviceName: element-web
          servicePort: {{ .Values.port }}
